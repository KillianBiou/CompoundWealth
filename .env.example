DATABASE_URL="file:./db/dev.db"
SESSION_SECRET=change_me_32_chars_minimum_secret

# Secret du recalcul planifié (POST /api/cron/refresh avec l'en-tête x-cron-secret)
# Sans cette variable, l'endpoint refuse de s'exécuter.
CRON_SECRET=change_me_too_32_chars_minimum
